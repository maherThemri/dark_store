import { RemoveExtraSpacesPipe } from './remove-extra-spaces.pipe';

describe('RemoveExtraSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveExtraSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
